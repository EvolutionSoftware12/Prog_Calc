version: '3.4'

services:
  calcolatrice2021:
    image: calcolatrice2021
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
